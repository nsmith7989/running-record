var React = require('react');

module.exports = React.createClass({

    render: function() {
        return (
            <footer>
                <span>Running Record</span>
            </footer>
        )
    }
});